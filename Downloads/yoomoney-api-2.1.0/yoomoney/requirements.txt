# ============================================================
# yoomoney-api — зависимости
# ============================================================
# Установка базовых зависимостей:
#   pip install -r requirements.txt
#
# С поддержкой Flask:
#   pip install -r requirements.txt -r requirements-flask.txt
#
# С поддержкой FastAPI:
#   pip install -r requirements.txt -r requirements-fastapi.txt
# ============================================================

# -- Обязательные ----------------------------------------
httpx>=0.27.0       # HTTP-клиент (sync + async)
pydantic>=2.0.0     # Валидация и модели данных
